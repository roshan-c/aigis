{
  "name": "aigis",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.37.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun --workspaces --filter discord-bot dev",
    "start": "bun --workspaces --filter discord-bot start",
    "build": "bun --workspaces --filter discord-bot build"
  }
}
